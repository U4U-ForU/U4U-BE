spring:
  application:
    name: ufu

  datasource:
    url: jdbc:mysql://localhost:3306/ufu_db
    username: root
    password: iokshoimong1!M
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

jwt:
  secretKey: ${JWT_SECRET_KEY:dWZ1LWp3dC1zZWNyZXQta2V5LWZvci1sb2NhbC1kZXZlbG9wbWVudC0yMDI2LXNlY3JldA==}
  accessExp: ${ACCESS_EXP:3600}
  refreshExp: ${REFRESH_EXP:1209600}
  header: ${HEADER:Authorization}
  prefix: ${PREFIX:Bearer }
